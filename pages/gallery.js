import styles from "../styles/Gallery.module.css";

const Gallery = () => {
  return (
    <main>
      <div className={styles.title_gallery}>Coming soon...</div>
    </main>
  );
};

export default Gallery;
